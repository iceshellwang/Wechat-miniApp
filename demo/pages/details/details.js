Page({
  data: {
 
},
  onLoad: function (options) {
    this.setData({
      aid:options.aid,
      name:options.name,
      price: options.price,
      kind: options.kind,
      sold: options.sold,
      grade: options.grade,
      longitude: options.longitude,
      latitude: options.latitude

    })},

    

    
})
  